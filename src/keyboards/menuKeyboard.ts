import { InlineKeyboard } from 'grammy';

export const menuKeyboard = new InlineKeyboard().text('🛍️ Товары', 'products').text('👤 Профиль', 'profile').text('🎬 Создать видео', 'generate')